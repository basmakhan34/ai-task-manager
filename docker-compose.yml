services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    # env_file:
    #   - .env # Commented out for easier deployment on Spaces
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:8000